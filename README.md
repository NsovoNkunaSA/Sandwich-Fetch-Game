
# Sandwich Fetch Game

A small JavaScript project for practicing **Promises, fetch(), `.then()`, and `.catch()`**.

## How It Works

The app fetches sandwich ingredients from a local `ingredients.json` file and uses them to create a sandwich.

## Technologies

* HTML
* JavaScript
* JSON
* Fetch API

## Run

Open the project using **VS Code Live Server** and click **Make Sandwich**.
